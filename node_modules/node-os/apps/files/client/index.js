var model = #import('model.js');

ko.applyBindings(model.viewModel, elem);

slots.getFiles(function (files) {
	_.each(files, function (file) {
		model.viewModel.files.push(new model.File(file));
	});
	model.viewModel.currentDir('/');
});

$(elem).mousedown(function (event) {
	model.viewModel.clearSelection();
});